# TASK: 쿠팡파트너스 완도 특산물 섹션 추가

> Claude Code에게: CLAUDE.md + CODING_RULES.md 읽고 아래 구현해줘.
> 완료 후 CLAUDE.md "🔴 지금 여기" 업데이트 + git commit 자동으로 해줘.

---

## 목표

항로 목록 아래, 면책 문구 위에
완도 특산물 쿠팡파트너스 배너 섹션을 추가한다.
배너는 1개씩 표시되며 5초마다 자동으로 교체된다.

---

## 구현할 파일

`components/CoupangSection.tsx` 신규 생성 후
`app/page.tsx` 에 import해서 적절한 위치에 삽입

---

## CoupangSection 스펙

### 레이아웃

```
┌─────────────────────────────────┐
│ 🦐 완도 특산물                  │
├─────────────────────────────────┤
│                                 │
│       [배너 1 or 2 교체]        │
│          320 x 100              │
│                                 │
│         ● ○ ○  (인디케이터)      │
├─────────────────────────────────┤
│ 이 섹션은 쿠팡 파트너스 활동의  │
│ 일환으로 수수료를 제공받습니다.  │
└─────────────────────────────────┘
```

### 배너 데이터

```tsx
const banners = [
  {
    src: "https://coupa.ng/cnzk5R",
    link: "https://link.coupang.com/a/eMkMaQmliK",
    label: "완도 특산물 1",
  },
  {
    src: "https://coupa.ng/cnzlh4",
    link: "https://link.coupang.com/a/eMxY1bJHrg",
    label: "완도 특산물 2",
  },
  {
    src: "https://coupa.ng/cnzlPC",
    link: "https://link.coupang.com/a/eMyd9PcYgK",
    label: "제주도 특산물",
  },
];
```

### 자동 교체 로직

- `useState`로 currentIndex (0, 1, 2) 관리
- `useEffect`로 5000ms 타이머 설정
- 컴포넌트 언마운트 시 타이머 반드시 cleanup

```tsx
useEffect(() => {
  const timer = setInterval(() => {
    setCurrentIndex((prev) => (prev + 1) % banners.length);
  }, 5000);
  return () => clearInterval(timer);
}, []);
```

### iframe 전환 방식 (중요)

`display:none` 사용 금지 → iframe 재로딩 발생
대신 CSS opacity + position absolute 방식으로:

```tsx
{
  banners.map((banner, i) => (
    <div
      key={i}
      style={{
        opacity: i === currentIndex ? 1 : 0,
        transition: "opacity 0.5s ease",
        position: i === 0 ? "relative" : "absolute",
        top: 0,
        left: 0,
      }}
    >
      <iframe
        src={banner.src}
        width="320"
        height="100"
        frameBorder="0"
        scrolling="no"
        referrerPolicy="unsafe-url"
      />
    </div>
  ));
}
```

### 인디케이터 (하단 점)

```tsx
<div className="flex justify-center gap-2 mt-2">
  {banners.map((_, i) => (
    <div
      key={i}
      className={`w-2 h-2 rounded-full transition-colors ${
        i === currentIndex ? "bg-blue-600" : "bg-gray-300"
      }`}
    />
  ))}
</div>
```

---

## 필수 고지 문구 (법적 의무 — 절대 생략 금지)

```
이 포스팅은 쿠팡 파트너스 활동의 일환으로,
이에 따른 일정액의 수수료를 제공받습니다.
```

폰트 크기: 11px, 색상: #4A5568, 섹션 하단 배치

---

## 디자인 가이드

- 배경: 흰색 카드 (#FFFFFF), border-radius: 12px
- 섹션 제목 "🦐 완도 특산물": 15px, #004B96, bold
- 배너 영역: position relative, overflow hidden
- 전체 너비: 모바일 100% (max-width 맞춤)

---

## page.tsx 삽입 위치

```tsx
<RouteList ... />
<CoupangSection />   {/* ← 여기 */}
<footer>면책 문구...</footer>
```

---

## 인수 조건

- [ ] 배너 1개가 표시되고 5초마다 자동 교체됨
- [ ] 전환 시 부드럽게 fade 효과
- [ ] 하단 인디케이터 점이 현재 배너 표시
- [ ] iframe 전환 시 재로딩 없음 (opacity 방식)
- [ ] 고지 문구 하단 표시
- [ ] 모바일(375px)에서 잘림 없이 표시
- [ ] 컴포넌트 언마운트 시 타이머 cleanup
